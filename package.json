{
  "name": "aloforge-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "lint": "echo \"TODO: add linting\"",
    "build": "pnpm -r build",
    "dev": "pnpm --filter web dev"
  }
}
